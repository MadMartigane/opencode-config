---
name: plan-build-classic
description: Build plan using architect in Classic mode (complete implementation-ready)
---

$1

Délègue la création du plan d'implémentation à l'agent `architect` via l'outil `task`.

**Instructions strictes à transmettre à `architect` (en anglais) :**

- **Mode**: Operate in "CLASSIC" mode.
- **Deliverable**: Produce a Complete Implementation Plan.
- **Precision**: Provide unambiguous specifications using code examples, interfaces, or strict functional specs. Leave ZERO implementation details to the `code-only` agent's discretion.
- **Execution Flow**: Define atomic tasks, explicit dependencies, and execution order (PARALLEL vs SEQUENTIAL).
